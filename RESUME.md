# RESUME — yolo_lpr_cpp の進捗と残作業

方針は [README.md](README.md)。ここは**現在地・次の一手・未確認事項**を書き続けるファイル。

## 現在地（2026-08-19 夜）

**3 段すべて自前の学習済みモデルで動いている。** 検出も認識も 4隅補正も学習を回し終え、
CLI・WASM の両方で実写が読める。借り物の検出器は「遠景が得意な選択肢」として残してあるだけ。

```
実写 640x480 ──▶ 検出 自前yolov8n(320) ──▶ box ──▶ 4隅補正 ──▶ 1クロップ ──▶ 11 head ──▶ 横浜 480 り 4567
det 0.93 / 地名 conf 0.94 / 0.44 秒（WASM, node, SIMD）── CLI と同じ文字列・同じ box
```

| 検証 | 結果 |
|---|---|
| 認識器 実データ hold-out（地域名） | **97.9%**（144 枚、margin 0.03、TTA なし）。ベースライン 92.4%。誤りは 3 枚 |
| 認識器 2025 追加の 5 地名 | **28/100**（精度を落とさず・デモ既定 v7_bal）／**76/100**（精度 95.1% を許す v6_last）。朝は 1/100 で確率 1e-09 |
| 認識器 138 地名を言えるか | 15.9% → **50.7%**（`tools/check_regions.py`、v6_last） |
| 検出器 学習時 val | **mAP50 0.985 / mAP50-95 0.947**（P 0.999 / R 0.987、合成 800 フレーム） |
| 検出器 recall（`data/det_eval` 175 個） | 320 で **93.7%**、640 で **97.1%**、借り物は 13.7%（このセットは車体なしが主） |
| 誤検出 | プレートが無い 12 フレームで **0 個**（借り物は 1 個） |
| 実写の黒ナンバー（車体なし・近接） | `横浜 480 り 4567`、det **0.93**、地名 conf 0.94、誤検出なし |
| 実写のバス（画面幅 11%） | `横浜 200 か 3591`、自前 640 で det 0.41 / 借り物で 0.80（自前 320 は落とす） |
| WASM（node、実写 2 枚） | CLI と同じ文字列・同じ box を assert して **PASS**、0.44-1.04 秒/フレーム |
| C++ ⇔ Python | 推論は文字列・argmax 一致、学習は step1 の loss 差 **1e-6**、生成は labels/meta がバイト一致 |
| 空クローンからの通し | clone 2 本 → ビルド → 生成 → 実写検出 → 学習 → パリティまで **通る**（`SETUP.md`） |
| ビルド | MSVC 14.41（vcvars 不要の `build/cc.sh`）・g++ 14.2・emcc（`-msimd128`）の 3 系統 |

**残っている弱点**（数字が出ているので優先度が判断できる）:
- **2025 追加地名**は 5 名すべて読めるようになったが、精度とのトレードオフが残る（97.9% なら 28%、
  95.1% なら 76%）。デモは両端を選べるようにしてある。詳細は下の「江戸川ナンバーは行ける？」節。
- `plate_kind` / `legible` head はまだ実質未学習（conf 0.40 / 0.57）。合成にはラベルがあるので学習は可能。
- 検出器は近接（320）と遠景（640）で 2 本立て。480 で 1 本化を試したが実写の遠景を落として却下。
- 実データが**長崎周辺に極端に偏っている**（佐世保 143 枚 / 福岡 75 / 長崎 75 で、63 地名中の上位 3 つが半分近く）。
  地域名 97.9% はこの偏りの上での数字なので、他地方の実写が手に入ったら再測定が必要。

**自作 ONNX インタプリタの数値誤差**（測って分かったこと）: 認識器（27 conv）で **3.3e-05**、
検出器（201 ノード・416×416）で **約 3e-03**。onnxruntime は同じ ONNX で 7.5e-09 なので、
差は自作側の naive な float 加算順序。強い検出には影響しないが、**閾値ぎりぎりの box では
読みが両実装で変わる**（実例: det 0.24 の誤検出が C++ 「なにわ」/ Python 「鹿児島」）。

**今すぐ効く弱点**（数字が出ているので直す優先度が判断できる）:
- 地域名 head の conf は 0.92 まで来た（検出器の差し替えで 0.57 から改善）が、まだ 1.00 ではない。→ 4隅補正（M6）。
- 検出器が**借り物**（PlateYOLO-JP, AGPL-3.0）。自前 yolov8n がこれを超えるのが M7 の合格条件。
- 近接・手持ちのプレートは依然として検出器の苦手領域なので、WASM デモの**手動枠**は残す。

調査で分かっている前提（再調査しないこと）:
- `yolov8_cpp` は v5/v8/v11/x 全サイズ、`yolo` CLI、`.pt` 読み書き、ONNX、CUDA 経路まで揃っている。
  **ただし cls head が 80 クラス固定**（同リポ RESUME #2）→ C++ 側でも nc=1 を学習するので**必須作業**（M7）。
- `lpr_cpp` は 9-head 分類器の forward parity 3.3e-05、pure C++ 学習ループ、実データローダ、WASM デモ、
  Eigen で約2倍。重み `pure/ref/weights.bin` は実データ学習済みの ONNX 由来（**元の .onnx は非公開**なので、
  本リポでは `jlpr export` で weights.bin から ONNX を作り直した）。
- 実データの当たり: `dyama/alpr_jp`(MIT) の `自家用/<地名>/` 720枚が**地域名ラベル付き**（66地名）、
  他に無ラベル 529枚、ネガ 4,420枚。
- 生成用書体の当たり: FGゼロラバウル（プレート書体再現・漢字かな入り・商用可フリー）。
- 地域名は現行 138 件（2025-05 に 十勝・日光・江戸川・安曇野・南信州 が追加）。既存 133 は完全な部分集合。
- emsdk は `C:\prog\emsdk\emsdk` に **install 済み**（`upstream/emscripten/emcc.py`、node は
  `node/22.16.0_64bit/bin/node.exe`）。`build/emcc.sh` がそこを直接叩く。

## マイルストーン

各段の「完了」は**数値で言えること**を条件にする。学習が絡む段は
**Python 実装・C++ 実装・両者のパリティ**の 3 点セットで完了とする（速度のパリティは求めない）。

- [x] **M0 足場** — `spec/` `pure/` `tools/` `wasm/` `models/` `build/`、MSVC/g++/emcc の 3 系統、stb 同梱。
- [x] **M1 ラベル表** — `spec/labels.txt` を実行時に読む方式（C++ `pure/spec.hpp` / Python `tools/labels.py`）。
      現行地名 138 を出典付きで固定、11 head ＋ 生成器専用ラベルを定義。
      パリティ: dump と emit-header が **バイト一致**（`python tools/parity/labels.py`）。
- [x] **M2 ワンパス推論（既存モデル・CLI + WASM）** — 統合 ONNX ランナ、weights.bin→ONNX エクスポータ、
      detect→crop→11head decode、ブラウザ demo（カメラ/ファイル/手動枠/PNG保存）と node テスト。
      数値は上の表。
- [x] **M3 Python 側の推論を対等にする** — `tools/infer.py`（onnxruntime）＋ `tools/jlpr.py detect`、
      前処理は `pure/crop.hpp` から式ごと移植。`jlpr detect --json` と JSON を突き合わせる
      `tools/parity/infer.py` を追加（head レベル＋ pipeline レベル）。**PASS**:
      強い box は 文字列・argmax 一致、box 差 0.10px、det 差 0.0000、地域名 conf 差 0.0108。
- [x] **M3.5 参考実装の評価と取り込み** — [PlateYOLO-JP](https://github.com/Kazuhito00/PlateYOLO-JP-Prototype)
      （YOLO12 検出器 + EkMixer 認識器, AGPL-3.0）を評価。認識器は自前が明確に勝ったので base 継続、
      検出器は借りて暫定に採用（NMS を `tools/strip_nms.py` で剥がし、`pure/nd.hpp` + `pure/infer_v8.hpp` を
      追加して自作ランナで実行）。副産物: 外部モデルのラベル空間を同じ形式で扱う `spec/ekmixer_labels.txt`、
      実データ評価ハーネス `tools/eval_ocr.py`。
- [x] **M4 合成データ生成器（両言語・認識用 crop）** — `pure/gen.hpp` + `pure/gen_render.hpp` + `jlpr gen`、
      `tools/gen.py`。中型/大型/図柄入り、色 4 種、浮き出し文字・光沢・ボルト・封印・フレーム・汚れ、
      ピンホール投影、劣化、JPEG 往復、**4隅 GT**。速度 0.04 秒/枚（OpenMP, 8 スレッド）。
      パリティ **PASS**: labels.txt / meta.txt が 2000 サンプルでバイト一致、4隅 0.0000 px 一致。
- [x] **M4b 検出用の合成データ（両言語）** — `pure/gen_det.hpp` + `jlpr gen-det`、`tools/gen_det.py`。
      プレート幅が画像幅の **3%〜95%**（対数一様）、0〜3 枚/フレーム、8% はハードネガ、背景は実写プール可。
      標準 YOLO 形式 + 正規化4隅。パリティ **PASS**（meta / corners / labels がバイト一致）。
      → 「プレート単体でも検出できる」ための学習データがこれで用意できた。
- [x] **M5 認識器の学習（両言語）** — 実装・パリティ・本番学習まで完了。
      - Python: `tools/ocr_model.py`（PyTorch 移植、ONNX 重み読み込み、**forward 3.275e-05 一致**）
        ＋ `tools/train_ocr.py`（マスク付き 11 head CE / hold-out 評価 / 3 種のチェックポイント書き出し）
      - C++: `pure/onnx_train.hpp` ＋ `jlpr train --model ocr` — **ONNX グラフをそのまま学習する**方式
        （アーキを二重に書かないので乖離しない）。head 拡張（133→138）とチェックポイント選択も実装済み
      - パリティ **PASS**: step1 の loss 差 **1e-6**、学習前の精度も一致（追加クラス bias -10 で 92.4% / 0 で 85.4%）
      - 実測（Kaggle T4、合成 3.3 万枚＋実データ 576 枚、2500 step、12 分）:
        実データ hold-out region **92.4% → 97.9%**（`models/plate_ocr_v4.onnx`、デモ既定）
      - 残り（M8 に送る）: 全文一致 ≥ 95% の確認、plate_kind / legible head（今も 0.40 / 0.57 でほぼ未学習）
- [x] **M6 4隅回帰器（両言語）** — 学習完了。合成 val 平均誤差 **1.93%**（目標 1.5% には未達だが実用域）。
      近接の実写で地域名 0.42 → **0.85**、90px 未満の box では使わない（実測で悪化するため）。
      作り直した v2 は合成 1.89% と改善したのに実写で 0.94 → 0.75 と悪化したので**不採用**。
      **学習は Python のみ**（`jlpr train --model corner` は未実装）＝対等性の唯一の穴。
- [x] **M7 検出器を自前 yolov8 nc=1 に差し替え** — 完了。実プレート貼り込み合成 9000 ＋ 疑似ラベル実車 588 枚、
      28 epoch（Kaggle T4、71 分）で **mAP50 0.985 / mAP50-95 0.947（P 0.999 R 0.987）**。
      `data/det_eval` のバケット別 recall: 320 で **93.7%**、640 で **97.1%**（借り物は同セットで 13.7%）。
      プレートが画面幅 3%〜95% のどこでも検出でき、**車体が無くても取れる**（実写の黒ナンバー 0.93）。
      完了条件のうち「色別 recall ≥ 0.90」だけ未測定（実写の黄/緑ナンバーが手元に無い）。
- [ ] **M7b C++ 側の検出器学習** — `yolov8_cpp` から v8 loss と TAL を移植する作業（約 1,100 行）。
      必要なのは `tal.hpp`(104) / `v8pure.hpp`(100) / `net.hpp`(136) / `net_unfused.hpp`(124) /
      `blocks.hpp`(70) / `dataset.hpp`(308) / `metrics.hpp`(93) / `ptio.hpp`(201) と、
      **cls head の nc=1 再初期化**（同リポ RESUME #2）。認識器と違い ONNX を直接学習する方式が使えない
      （v8 の損失は生の head 出力を必要とし、グラフの外側にあるため）。
- [~] **M8 WASM 仕上げ** — GitHub Pages 公開済み（`https://yomei-o.github.io/yolo_lpr_cpp/wasm/`）、
      検出器 3 択（借り物 / 自前320 / 自前640）、トラッキングで自動スキャン、1 フレーム 0.44-1.04 秒を実測掲載。
      `wasm/test_node.js` は実写 2 枚のフィクスチャで CLI と一致（PASS）。
      残り: **ブラウザ実機での再確認**（モデルを差し替えたので）、plate_kind / legible の学習、
      **2025 追加地名の仕上げ**（十勝が残っている。下の「江戸川ナンバーは行ける？」節）。
- [ ] **M9 リリース** — `models/` に 3 つの ONNX、両言語の `val` が同じ数値、色別・地名別・解像度別の評価表。

## 次の一手（2026-08-19 夜の引き継ぎ）

**環境**: このリポジトリと `git clone --depth 1 https://github.com/dyama/alpr_jp.git ../alpr_jp` の
2 本だけで生成・学習・評価・WASM が全部通る（空クローンで実測確認済み）。手順は `SETUP.md`。
Kaggle GPU は README の kbridge 節（学習 job の先頭は必ず
`git checkout -- models/; git clean -fdq models/; git pull`）。

1. **「76% を 97.9% の精度で」** — 今日見えたフロンティアの内側を取る作業。
   `97.9% ↔ 28%`（v7_bal・デモ既定） / `95.8% ↔ 50%`（v7_last） / `95.1% ↔ 76%`（v6_last）。
   効きそうな順に:
   - 実データが 1〜3 枚しかない地名（春日部・神戸・多摩など）が壊れやすいので、**その地名だけ
     margin/TTA を変えて読む**か、実データ側を augment で増やす。v6_last の劣化 4 件中 3 件がここ。
   - **新5名の合成を実物寄りにする**。いまは素の書体で描いているだけで、実プレート写真の貼り込みは
     検出器のデータ（`--real-plates`）にしか入っていない。認識器側にも同じ仕掛けを入れられる。
   - **region head だけ学習率を下げて長く回す**（`--backbone-lr-mult` の逆で head 側を絞る口が必要）。
2. **`plate_kind` / `legible` head** — 長い学習で plate_kind が 57% → **76.7%** まで伸びた（副産物）。
   legible は 0.57 のまま。合成にはラベルがあるので、この 2 head を主役にした学習を 1 本回せば片付く。
3. **ブラウザ実機での確認**（node では PASS だが実機は未確認）。検出器 3 択 × 認識器 2 択、
   自動スキャン、手動枠。`https://yomei-o.github.io/yolo_lpr_cpp/wasm/`
4. **色別 recall（白/黄/緑/黒）** が M7 の完了条件のまま未測定。実写の黄・緑ナンバーが必要。
   合成の色替えは簡単すぎて役に立たないことは実測済み（README の落とし穴）。
5. **M7b C++ 側の検出器学習**（v8 loss + TAL の移植、約 1,100 行）。ここだけ対等性が大きく欠けている。
   4隅回帰の学習（`jlpr train --model corner`）も Python のみ。
6. 却下済みなので触らないこと: 検出器 480 での 320/640 統合（合成では良いが実写の遠景を落とす）、
   4隅回帰 v2（合成 1.89% でも実写で悪化）、書体を増やして地名を改善（差は 1.4pt しかない）。

## 合成データのドメインギャップ — 2026-08-19 実測

生成した合成プレートを**実データ学習済みの現行認識器**に読ませると（`tools/eval_ocr.py --kind synth`、
劣化なし `--clean`、200-400枚）:

| フォント（1 種に固定して比較） | 全文一致 | region | 分類番号 3桁 | ひらがな | 一連番号 4桁 |
|---|---|---|---|---|---|
| GenSenRounded2-B | 8.0% | 26% | 86/91/92% | 69% | 85/83/88/90% |
| YuGothB | 9.0% | **29%** | 92/90/92% | 88% | 86/77/85/85% |
| meiryo | 2.0% | 29% | 72/75/58% | 57% | 66/89/86/93% |
| msgothic | 2.5% | 28% | 84/80/73% | 71% | 86/72/74/74% |

読み方:
- **数字・アルファベットは 72-92% 転移する**。合成の形は実物に十分近い。
- **地域名は 26-29% で頭打ち、フォントを変えても動かない**（当てずっぽうは 0.7% なので転移はしている）。
  138 クラスを 30px 前後の漢字 2 文字で決める head なので、**書体の字形差がそのまま効く**。
  本物のプレート書体は専用設計で、フリーフォントでは埋まらない。
- したがって region の最終精度は**実データでの微調整**が担う（M5）。合成は事前学習と他 head 用、
  という役割分担がはっきりした。実データ 720 枚（地域名ラベル付き）＋疑似ラベルをそこに使う。
- 逆に言うと、合成だけで学習した数字系 head は最初から実用域に入る見込み。

## 検出器は「車体込みの小さいプレート」しか取れない — 2026-08-19 実測

`tools/context_test.py`（同じ写真、プレートを中心に切り出す窓の大きさだけを変える）:

| プレートが窓幅に占める割合 | 5% | 8% | 12% | 20% | 30% | 45%以上 |
|---|---|---|---|---|---|---|
| 現行 PlateYOLO-JP 320 のスコア | 0.757 | **0.826** | 0.695 | 0.069 | なし | なし |

**借り物の検出器 2 つとも同じ性質**（旧 YOLOX も最良帯 5–12%）で、これは学習データの分布そのもの。
つまり手持ちで寄って撮ると検出できないのは仕様どおりで、閾値を下げても直らない。

- WASM デモ: 実測帯をページに明記し、「枠を手で指定」を常設（認識器は近接でも読める）。
- M7 の合格条件に **画面幅 5%–95% のどこでも検出**を追加。合成コンポジットで近接も学習させる。

## 実写の黒ナンバーで決着 — 2026-08-19（自前検出器がプレート単体を取れた）

ユーザ提供の実写（軽事業用・黒地黄字 `横浜 480 り 45-67`、**車体なし・机の上**、画面幅の 24%）:

- **借り物 PlateYOLO-JP: 検出ゼロ**（車体込み前提という実測どおり）
- **自前 yolov8n（合成データのみ・学習途中）: det 0.80 で検出し、読みも「横浜 480 り 4567」で正解**

スケール別（`tools/context_test.py`、同じ写真で切り出し窓だけ変える）:

| プレートが窓幅に占める割合 | 5% | 8% | 12% | 20% | 30% | 45% | 60% | 80% | 95% |
|---|---|---|---|---|---|---|---|---|---|
| 借り物 PlateYOLO-JP | 0.76 | 0.83 | 0.70 | 0.07 | なし | なし | なし | なし | なし |
| 自前 yolov8n（合成のみ） | 0.09 | 0.42 | 0.66 | 0.83 | 0.85 | 0.85 | 0.86 | 0.89 | 0.90 |

**2 つは相補的**。合成データを 3–95% の対数一様で振った狙いがそのまま出た形で、
「プレート単体でも検出したい」という要件は自前モデルで達成できた。残りは遠景（5–8%）で、
これは合成の背景が車体でないことが原因なので、実プレートの貼り込み（`--real-plates`）と
実写の教師ラベルで詰める。

なおこの測定で自分の書いた罠を踏んだ: `tools/context_test.py` に `--fmt` を付け忘れて
cxcywh の box を xyxy として読み、「検出ゼロ」と誤って報告した（もっともらしい箱が変な位置に出る）。
両ツールに `--fmt` を追加した。

## プレート色（黒ナンバー）の現状 — 2026-08-19 実測

`tools/recolor_test.py`（同じ写真・同じ幾何、プレート内の輝度を min-max 正規化して地色/文字色を差し替え、
陰影とにじみは保存）:

| 地色/文字色 | 現行 PlateYOLO-JP 320 | 旧 YOLOX-tiny 416 | 同じ box を渡したときの読み |
|---|---|---|---|
| 白地緑字（自家用） | 0.796 | 0.800 | 横浜 200 か 3591（地域名 conf 0.91） |
| 緑地白字（事業用） | 0.799 | 0.722 | 同（1.00） |
| 黄地黒字（軽自家用） | 0.721 | 0.645 | 同（0.97） |
| **黒地黄字（軽事業用）** | **0.835** | 0.720 | 同（0.99） |

結論を 2 つに分けて書く:
- **認識器は 4 色すべて問題なく読めている**（同じ box を渡せば conf 0.91-1.00）。色は認識側のボトルネックではない。
- **検出器の色バイアスは、この方法では再現しない**。lpr_cpp が報告した「黒 0.21」は当時の色替え方法か写真に
  依存していた可能性が高く、現行検出器の 0.835 を「直った」と言い切るのは早い。判定には**実写の黒/黄ナンバー**が必要
  （データ不足。撮影 1-2 枚あれば決着する）。

## トラッキング（自動スキャン）— 2026-08-19

`pure/track.hpp`: IoU で同一プレートを追跡し、**per-head 確率をフレーム方向に足し込む**。
マージン TTA と同じ投票を時間軸でやるだけなので追加コストはゼロ（フレームは勝手に来る）。
N フレーム連続で読みが変わらなければ「確定」。WASM の `jl_track_step` から使い、デモの
「自動スキャン」がこれで動く。推論は Web Worker に移した（1 フレーム 0.3-1.5 秒なので
メインスレッドではプレビューが固まる）。

実測（実写 640x480、node/SIMD、自前検出器）: 初回 1.16 秒、以降 **0.56-0.61 秒/フレーム**、
3 フレーム目で確定。

## 学習の到達点（2026-08-19 時点）

| モデル | 状態 | 数字 |
|---|---|---|
| 認識器 `plate_ocr_v2.onnx` | 学習済み | 実データ hold-out の地域名 **97.9%**（ベースライン 92.4%）、11 head。ただし地名は実データが持つ 63 名しか言えない |
| 認識器 `plate_ocr_v3.onnx` | 学習済み（地名カバレッジ版） | 実データ 97.9% を維持したまま、138 名レンダの正解率 15.9% → **47.1%**。2025 追加の 5 名はまだ 0 |
| 4隅回帰 `plate_corner.onnx` | 学習済み（合成 val 平均誤差 1.93%） | 近接では地域名 0.42→**0.85**、小さい box では逆効果なので 90px 未満は使わない |
| 4隅回帰 `plate_corner_v2.onnx` | 学習済みだが**採用しない** | 合成 val の誤差は 1.93% → **1.89%** と改善したのに、実写の黒ナンバーでは地名確信度 0.94 → **0.75** と悪化。合成の指標が実写の指標と逆を向いた例。比較できるようリポジトリには置いてある |
| 検出器 `plate_det_v8n_320.onnx` | **再学習済み・既定** | 学習 val mAP50 0.985 / mAP50-95 0.947、`data/det_eval` recall **93.7%**、実写の黒ナンバー近接 **0.93**（誤検出なし） |
| 検出器 `plate_det_v8n_640.onnx` | **再学習済み** | 同セット recall **97.1%**。画面 5-10% の小さいプレートで 320 の 86.8% に対し 94.7%。バスの実写（11%）は 640 だけが取れる |
| 検出器（借り物 PlateYOLO-JP） | 参照 | 同セットでは 13.7%（このセットは車体なしが主なので不利）。遠景の実写では 0.80 と今でも最強 |

検出器のデータを作り直した理由: 描いた図版だけの合成では**合成 val で mAP50 0.99 なのに実写で 0.1**。
実物の書体・汚れ・照明が要る。そこで
- `--real-plates`: 実プレート写真 1,273 枚をテクスチャとして貼る（全スケール 3-95%）
- `tools/pseudo_label.py`: **実車写真 1,309 枚**（alpr_jp/train/sample にあった）を借り物検出器で
  自動ラベル → 588 枚が conf≥0.5 で採用。借り物が強い 5-12% 帯だけを教師にするので役割が噛み合う
- 実車写真は**背景には使わない**（写っている本物のプレートが無ラベルになり「プレートは背景」と教えてしまう）

## 「江戸川ナンバーは行ける？」— 2026-08-19 実測と対策

答え: **出荷モデル（v2）では絶対に出せなかった**。spec には 138 地名あるのに、
十勝 / 日光 / 江戸川 / 安曇野 / 南信州 の確率は **1.0e-09（138位中136位）** で固定されていた。

原因は 3 つの設計判断が積み重なった結果で、どれも単体では正しかった:
1. 実データ（alpr_jp）が持っている地名は **63 名だけ**。新 5 名の写真は 1 枚も無い。
2. 合成データでは region head を**マスク**している（合成の字形は転移せず、実データが学んだ重みを壊すため）。
3. 追加クラス（133→138）の bias を **-10** にしていた（学習しないクラスが 0 だと実クラスに勝ってしまうため）。
→ どこからも勾配が来ないクラスに、10 logit の借金だけが残った。

対策と実測（`tools/check_regions.py`：地名ごとに 1 枚レンダして読ませる。138 名 × 2 枚）:

| | 138名の正解率 | 実データがカバーする63名 | 合成のみの75名 | 江戸川の確率 |
|---|---|---|---|---|
| v2（出荷モデル） | 15.9% | 23.0% | 10.0% | 1.0e-09（136位） |
| v3（実データに無い地名だけ合成で教える） | 47.1% | 33.3% | 58.7% | 1.0e-07（133位） |
| v4（＋追加クラスの bias を 0 に） | 47.5% | 33.3% | 59.3% | **5.5e-04（26位）** |
| v5_last（＋新5名を全体の 10% まで増量） | 47.1% | 31.0% | 60.7% | **2.7e-02（6位）** |

同じ測定を**学習と同じ書体集合**（`spec/fonts.txt` の 2 書体）で作った `data/region_sweep_c` でやると:

| | 138名 | 実データがカバーする63名 | 合成のみの75名 | 2025追加の5名 |
|---|---|---|---|---|
| v4 | 48.9%（未見書体では 47.5%） | 34.1% | 61.3% | 全部 0/2 |
| v5_last | **50.4%** | 31.0% | **66.7%** | **安曇野 2/2、江戸川 1/2、日光 1/2、南信州 1/2、十勝 0/2** |

**書体の影響は 1.4 ポイントしかない**（未見の Windows 書体 3 つで測っても 47.5% vs 48.9%）。
つまり地名 head のボトルネックは書体ではなく露出量。書体を増やす方向の改善は費用対効果が悪い。
逆に言えば、`spec/fonts.txt` の 2 書体で学習して Windows 書体で評価する（＝未見書体でのテスト）が
そのまま成立している。

実データ hold-out の地域名精度は v2/v3/v4 すべて **97.9%**（＝この対策で実データ性能は落ちていない）。
新 5 名だけ遅れているのは、他の未カバー地名が元モデルの重みを持っている一方で、
この 5 つは**重み 0 から**始まるため。だから足りないのは露出量で、`--region 133-137` で増量する。

### 新5名だけを 20 枚ずつ測る（2 枚では判定できないと分かったので）

`jlpr gen --out data/new5 --count 100 --region 133-137 --seed 777 --fonts-strict` の 100 枚:

| モデル | 実データ hold-out | 十勝 | 日光 | 江戸川 | 安曇野 | 南信州 | 新5名 計 |
|---|---|---|---|---|---|---|---|
| v4（実データ重視・デモ既定） | **97.9%** | 1/20 | 0/20 | 0/20 | 0/20 | 0/20 | 1/100 |
| v5_last（新5名 10%・3000step） | 97.2% | 6/20 | 4/20 | 6/20 | 4/20 | 8/20 | 28/100 |
| v6（新5名 17%・best=step750） | 97.2% | 10/20 | 3/20 | 5/20 | 5/20 | 3/20 | 26/100 |
| **v6_last（新5名 17%・step5000）** | 95.1% | **16/20** | **13/20** | **12/20** | **18/20** | **17/20** | **76/100** |

**露出量が足りていなかっただけ**で、6× の増量＋長い学習で 1% → 76% になった。
代償は実データ hold-out の 2.8 ポイント（144 枚中 4 枚）。その 4 枚を 1 枚ずつ見ると:

```
v4       の誤り 3件: 佐賀->諏訪(学習25枚)  大分->長崎(12枚)  熊本->筑豊(17枚)
v6_last  の誤り 7件: 上の3件 ＋ 春日部->名古屋(1枚)  神戸->伊勢志摩(1枚)  多摩->沖縄(3枚)  長崎->大阪(75枚)
```

増えた 4 件のうち 3 件は**学習 1〜3 枚しかない地名**だが、1 件は 長崎（75 枚）で本物の劣化。
ノイズと言い切れないので**単純入れ替えはせず、デモで認識器を選べるようにした**（v4 / v6_last）。

### v7: 実データ比率 0.55 ＋ balanced 選択 — **両取りできた**（2026-08-19 18:24）

| モデル | 実データ hold-out | 十勝 | 日光 | 江戸川 | 安曇野 | 南信州 | 新5名 |
|---|---|---|---|---|---|---|---|
| v4（旧デモ既定） | 97.9% | 1/20 | 0/20 | 0/20 | 0/20 | 0/20 | 1/100 |
| v7（best=step750） | 97.9% | 8/20 | 3/20 | 2/20 | 2/20 | 4/20 | 19/100 |
| **v7_bal（balanced=step1500）** | **97.9%** | 8/20 | 3/20 | 6/20 | 5/20 | 6/20 | **28/100** |
| v7_last（step4000） | 95.8% | 12/20 | 7/20 | 7/20 | 11/20 | 13/20 | 50/100 |
| v6_last（実データ比率 0.4） | 95.1% | 16/20 | 13/20 | 12/20 | 18/20 | 17/20 | **76/100** |

**`--export-balanced` が狙い通りに効いた**: v7_bal は hold-out の誤りが v4 と**完全に同一の 3 件**
（佐賀→諏訪 / 大分→長崎 / 熊本→筑豊）で、劣化ゼロのまま新 5 名が 1/100 → 28/100。
なので v4 は用済み、デモの「実データ重視」を v7_bal に差し替えた。

見えたフロンティア（実データ精度 ↔ 新地名カバレッジ）:
`97.9% ↔ 28%`（v7_bal） / `95.8% ↔ 50%`（v7_last） / `95.1% ↔ 76%`（v6_last）。
デモでは両端を選べるようにしてある。**76% を 97.9% の精度で出すのが次の課題**で、
効きそうな順に ①実データの少ない地名（1〜3枚）の TTA/margin を見直す
②新5名の合成をもっと実物寄りにする（今は素の書体で、実プレートの貼り込みは検出器側にしか入っていない）
③region head だけ学習率を下げて長く回す。



なお合成レンダは region head にとってドメイン外（実データ専任で学習してきた）なので、
「138名の正解率」は実世界の精度ではなく**そのクラスに到達できるか**の指標として読む。

## Kaggle GPU での学習（2026-08-19、kbridge 経由）

[kaggle_server_cpp](https://github.com/yomei-o/kaggle_server_cpp) の kbridge でローカルから Kaggle の
無料 GPU を叩いて学習した。手順は README「Kaggle の GPU で学習する」。実測:

| | |
|---|---|
| 環境 | Tesla T4 ×2 / 4 vCPU / 20GB / torch 2.10+cu128 / インターネット可 |
| 合成データ生成 | 32,000 枚を 4 コアで約 15 分（`/job` で投げて放置） |
| 認識器の学習 | 4,000 step (batch 64) が約 25 分。ローカル CPU の 40 倍以上 |
| 実データ hold-out region | 91.7%（出荷済み） → **97.9%**（step 500-1000） |

**学んだこと**: 実データが 576 枚しかないので step 1000 以降は過学習で下がる（97.9% → 95.8%）。
そこで **hold-out が最良の時点を保存して export** する仕組みと、実データ crop への
margin/brightness/contrast ジッタを入れた（C++ 側にも同じ 3 draw を入れてパリティ維持）。
また 4 コアしかないので、データローダを並列化しないと GPU 利用率が 28% で止まる（並列化後 64%）。

## 未確認事項（着手前に潰す）

1. **Roboflow の日本プレート dataset のライセンス** — `universe.roboflow.com` が 403 で未取得。
   商用不可なら検出は Open Images + 合成 + 自前撮影に切り替える（方針自体は変わらない）。
2. ~~**書体の再配布可否**~~ — **解決（2026-08-19）**: 実際に使っている 2 書体はどちらも再配布可なので
   同梱した（`GenSenRounded2-B.ttc` = SIL OFL 1.1、`DroidSansFallbackFull.ttf` = Apache-2.0 / (c) Google）。
   `spec/fonts.txt` がその集合の契約で、`--fonts-strict` で固定できる。FGゼロラバウルは未使用のまま
   （プレート書体の再現度は高いが、再配布可否を読み切れていないので手を出していない）。
   Windows 同梱の商用書体（meiryo 等）は**未見書体での評価**専用に格下げ（学習には使わない）。
3. ~~**地名の現行リスト**~~ — **解決（2026-08-19）**: 現行 138 件（うちご当地 51 件）を Wikipedia
   「日本のナンバープレート地域名一覧」から機械抽出。既存 133 は完全な部分集合で廃止・改称なし、追加は
   **十勝・日光・江戸川・安曇野・南信州** の 5 件だけ。index 0..132 固定＋末尾追記で事前学習 head を活かせる。
   残る確認: 図柄入りで字体・字配置が変わるものがないか（M4 の生成器で実物と見比べる）。
4. **図柄入りナンバーの背景をどう合成するか** — 実物の図柄は著作権があるので**模倣柄で近似**する方針。
   実写での通用性は M5 の評価で測る。
5. **plate_kind head の切り方** — 8 クラス 1 本で足りるか、色 / 大型 / 図柄を独立 head に割るか。
   M4 の生成器で両方のラベルを出しておけば後から選べる。
6. **WASM の速度** — 4隅補正が入って 1 フレーム **0.44 秒**（近接、320、1 クロップ）/ 1.04 秒（遠景、
   借り物 320 ＋ 6 クロップ TTA）まで来た。`nd.hpp` の N次元 op は素朴なループなので SIMD が
   効いていない可能性は残る。**ブラウザ実機での実測とプロファイルは未実施**。
7. ~~**20 MB の検出器**~~ — 解決: 320px 版に替えて **10 MB**。fp16 化でさらに半分にできる（M8 で検討）。
8. ~~**借り物の検出器をどう扱うか**~~ — **解決（2026-08-19）**: 自前が近接で明確に超えた（実写の黒ナンバー
   0.93 対 0 detections）ので既定を自前に差し替え。ただし**遠景では借り物が今も最強**（画面 11% で 0.80 対
   自前640 の 0.41）なので、消さずにデモの選択肢として残す。AGPL-3.0 は `THIRD_PARTY_NOTICES.md` に明記。
9. **色別 recall（白/黄/緑/黒）が未測定** — M7 の完了条件に入れていたが、実写の黄・緑ナンバーが手元に無い。
   合成の色替えは簡単すぎて役に立たないことが分かっている（README の落とし穴）。実写を集めるのが先。
10. **実データの地域偏り** — 佐世保 143 / 福岡 75 / 長崎 75 で、63 地名の上位 3 つが実データの半分近い。
   地域名 97.9% はこの偏りの上の数字。他地方の実写が入ったら再測定する。

## ログ

- **2026-08-19** — 方針決定。3段パイプライン（検出 yolov8n nc=1 → 4隅補正 → 11 head 分類）、
  成果物は全段 ONNX、対応は中型＋大型＋軽＋図柄入り、事前学習は AGPL 許容、WASM デモ込み。
  リポジトリ作成、README / RESUME 起稿。
- **2026-08-19（追記）** — 「**Python と C++ でどちらでも同じことができる**」を設計の核に昇格。
  `spec/` を唯一の定義とし、パリティテストで縛る。マイルストーンを 3 点セット方式に組み替え。
- **2026-08-19（M1）** — `spec/labels.txt` 起草（11 head ＋ 生成器専用ラベル）。現行地名 138 を一次情報から確定。
  `class_num_03` は既存モデル由来の未使用 1 枠があり（トークン 21 に対し 22 クラス）、互換のため `<unused>` で残す。
- **2026-08-19（M0/M1 完了）** — エンジンを姉妹リポから移植、`pure/spec.hpp` ＋ `tools/labels.py` を
  実行時パース方式で実装、パリティ PASS。`build/cc.sh` が vcvars なしで MSVC を叩けることを確認。
- **2026-08-19（M2 完了・最初のゴール）** — ワンパス推論。統合 ONNX ランナ、`jlpr export` で
  weights.bin→ONNX（元 ONNX と 3.299e-05 一致）、`jlpr detect` で実写を読み、同じコードを WASM 化して
  node で同一結果。**優先順位を「まず WASM で推論できるワンパス」に変更したのに合わせ、学習系は M4 以降へ後ろ倒し。**
- **2026-08-19（M3 完了）** — Python 推論（onnxruntime）を追加して両言語で同じ ONNX・同じ decode に。
  パリティを 2 段構えにした: **head レベル**（fixture 相手。ORT 7.451e-09 で、エクスポータの正しさと
  自作インタプリタの誤差 3.3e-05 を分離できた）と **pipeline レベル**（JSON 突き合わせ）。
  低スコア box で読みが割れるのは地域名 head の不安定さそのもので、M6 の 4隅補正で潰す対象として記録。
- **2026-08-19（M3.5）** — 参考実装 PlateYOLO-JP-Prototype を評価。**認識器は自前が勝った**
  （720 枚実データの地域名 top1: 自前 92.5% vs EkMixer 81.2%、TTA 時）ので base は変えない。
  **検出器は借りた**（YOLO12, AGPL）: NMS 入りの ONNX は自作ランナで実行不能だったので、NMS 手前で
  グラフを切って（`tools/strip_nms.py`）N次元 op（`pure/nd.hpp`）と v8 decode（`pure/infer_v8.hpp`）を
  実装して実行。結果、同じ写真で誤検出 1→0 件、地域名 conf 0.57→0.92、CLI 5.6→2.4 秒。
  **box の質が認識精度を支配する**ことが数字で出たので、M6（4隅補正）の位置づけがより明確になった。
  外部モデルのラベル空間（region 139・index 6 未使用）も現行 138 地名と完全一致で、我々のリストの独立確認になった。
- **2026-08-19（M4 / M4b 完了）** — 合成データ生成器を両言語で実装し、ラベルと幾何のバイト一致を達成。
  認識用 crop（4隅 GT 付き）と検出用フルフレーム（3%〜95% の全スケール＋ハードネガ）の 2 系統。
  実測で分かったこと 3 つ: ①**検出器は車体込みの小さいプレート専用**（20% で 0.07、30% 以上ゼロ）で、
  これは借り物 2 つとも同じ → 学習データで埋める方針を M4b で用意。②**合成の数字は 72-92% 転移するが
  地域名は 26-29% で頭打ち**、フォントを変えても動かない → region は実データ微調整が担う。
  ③色バイアスは陰影を保つ色替えでは再現せず（黒 0.835）、判定には実写の黒ナンバーが必要。
- **2026-08-19（M5）** — 認識器の学習が両言語で動き、パリティも取れた。C++ 側は
  **ONNX グラフを直接学習する**方式（初期化子を永続 Tensor にして backward → Adam → 書き戻し）にしたので、
  アーキテクチャの二重定義が無い。実測: 実データ hold-out region **91.7% → 95.1%**（600 step, CPU）。
  途中で見つけた重要なバグ 1 つ: **head を広げるとき新クラスの bias が 0 だと既存クラスに勝ってしまい、
  学習前から精度が 6 ポイント落ちる**（logit 0 vs 負の logit）。−10 で初期化して解決。
  検出器側は評価ハーネス（バケット別 recall）と学習スクリプト・notebook を用意。借り物のベースラインは
  合成データで全体 9.1%・35% 超 0.0% なので、埋めるべき穴がはっきりしている。
- **2026-08-19（検出器の再学習完了・M7）** — 実プレート貼り込み合成 9000＋疑似ラベル実車 588 枚で 28 epoch（Kaggle T4、71 分）。
  **mAP50 0.985 / mAP50-95 0.947（P 0.999 R 0.987）**、`data/det_eval` recall 93.7%（320）/ 97.1%（640）。
  実写の黒ナンバー近接は 0.80 → **0.93** になり、モニタ縁の誤検出も消えた。バスの遠景（画面 11%）は
  320 では落ちるが 640 なら 0.41 で読める。デモに自前 640 を追加して 3 択にした。
  学習直後に export だけ落ちた（`model.trainer.best` を使っていなかった）→ `--export-only` で学習なしに復旧。
  `simplify=True` を外すと Shape/Gather が残って自作インタプリタで読めないことも再確認。
- **2026-08-19（地名カバレッジ）** — 「江戸川は行ける？」に対する答えは No（確率 1e-09）だった。
  実データに無い地名だけを合成で教える／追加クラスの bias を -10 → 0 にする／新 5 名を全体の 17% まで
  増量する、の 3 段で新 5 名は **1/100 → 76/100**（各名 20 枚で測定）。
  `tools/check_regions.py` を受け入れテストとして追加、`--region` を両言語の生成器に追加。
  C++ にも head 拡張（`onx::widen_heads`）を実装したので、学習側もこれで本当に両言語対等になった。
- **2026-08-19（リポジトリ整備）** — 再配布可能な GenSenRounded2-B.ttc（OFL）を同梱。
  `tools/make_data.sh` で全データセットを種つきで再生成できるようにし、
  「何を git に入れて何を入れないか」を README に表で明記した。
- **2026-08-19（地名と精度の両取り・締め）** — 実データ比率 0.4 → 0.55 にして、今日実装した
  `--export-balanced`（実データを best-1.5pt 以内に抑えて合成地名を最大化）で取った **v7_bal** が
  **実データ 97.9%・hold-out の誤りは v4 と完全に同一の 3 件**のまま、新 5 名 1/100 → **28/100**。
  劣化ゼロなので v4 を置き換えてデモ既定にした。カバレッジ最優先の **v6_last（76/100・97.9→95.1%）** は
  ページ上で選べる 2 つ目の認識器として併置。見えたフロンティアは `97.9%↔28% / 95.8%↔50% / 95.1%↔76%`。
  WASM を今日のソースで再ビルドし、検出器 3 択 × 認識器 2 択で node のフィクスチャ 2 本が PASS。
  リポジトリ整備の締めとして書体 2 つ（OFL + Apache-2.0）を同梱、`--fonts-strict` で公開データを固定、
  `SETUP.md`／`requirements.txt`／`tools/make_data.sh` を追加して**clone 2 本だけで学習できる**状態にした。
