# RESUME — yolo_lpr_cpp の進捗と残作業

方針は [README.md](README.md)。ここは**現在地・次の一手・未確認事項**を書き続けるファイル。

## 現在地（2026-08-19）

技術方針の確定まで。コードはまだ 1 行も無い。確定内容は README の「確定した方針」表と
「Python と C++ の対等性」表（**Python と C++ で同じことができる**のが本リポの主眼。
片方だけの機能は作らない）。
調査で分かっている前提（再調査しないこと）:

- `yolov8_cpp` は v5/v8/v11/x 全サイズ、`yolo` CLI（train/val/detect, data.yaml, mosaic/mixup/HSV/affine）、
  `.pt` 読み書き、ONNX リーダ／エクスポータ、CUDA(Thrust/cuBLAS) 経路まで揃っている。
  **ただし cls head が 80 クラス固定**（同リポ RESUME #2）→ C++ 側でも nc=1 を学習するので**必須作業**（M6）。
- `lpr_cpp` は 9-head 分類器の forward parity 3.3e-05、pure C++ 学習ループ、実データローダ（`labels.txt`）、
  WASM デモ、Eigen で約2倍、`lpr_infer` まで動いている。重み `pure/ref/weights.bin` は実データ学習済みの ONNX 由来。
- 実データの当たり: `dyama/alpr_jp`(MIT) の `自家用/<地名>/` 720枚が**地域名ラベル付き**（66地名）、
  他に無ラベル 529枚、ネガ 4,420枚。
- 生成用書体の当たり: FGゼロラバウル（プレート書体再現・漢字かな入り・商用可フリー）。
- 地域名は 2025-05 に 十勝・日光・江戸川・安曇野・南信州 が追加。既存 133 クラスは拡張が必要。

## マイルストーン

各段の「完了」は**数値で言えること**を条件にする（姉妹リポと同じ流儀）。
さらに全段で **Python 実装・C++ 実装・両者のパリティテスト**の 3 点セットが揃って初めて完了とする。

- [ ] **M0 足場** — `spec/` `pure/` `tools/` `wasm/` `models/`、ビルドスクリプト（cl / g++ / emcc）、
      同梱物（stb 等）、`.gitignore`、パリティテストの雛形（`tools/parity/`）。
      完了条件: 空の `jlpr`（C++）と `tools/` の CLI が同じサブコマンドを受け付け、3 系統でビルドできる。
- [ ] **M1 ラベル表の確定** — `spec/labels.txt` を唯一の定義とし、そこから `pure/lpr_labels.hpp` と
      `tools/labels.py` を**生成**する。現行地名（138+）を出典付きで固定、ひらがな使用可集合、分類番号の桁ルール、種別 8 クラス。
      完了条件: 既存 133 との差分が列挙され、C++ と Python のラベル表・decode 結果が**バイト一致**。
- [ ] **M2 合成データ生成器（両言語）** — `tools/gen.py` と `pure/gen.cpp`。テンプレート・書体・物理劣化・4隅 GT・全 head ラベル。
      完了条件: 同じ seed で**文字列・4隅・変換パラメータ・色・種別が完全一致**（画像はラスタライザ差を許容し統計＋目視）。
      生成→デコードで元の文字列に戻る自動チェック。各 1,000 枚を本物と混ぜて見分けにくい水準。
- [ ] **M3 認識器の3者一致** — 既存 ONNX の重みを PyTorch 再実装（`ARCH.md` 準拠）と C++ forward（`lpr_cpp` 由来）に読み込む。
      完了条件: **ONNX / PyTorch / C++ の 9 head 出力が互いに 1e-5 以内**（`lpr_cpp` 実績 3.3e-05 と同水準）。
- [ ] **M4 認識器の学習（両言語）** — 合成事前学習 → head 拡張（11 head）→ 実データ微調整 → 両言語から ONNX 出力。
      完了条件: 実データ hold-out でプレート全文一致 ≥ 95%、region ≥ 98%（TTA なし）。
      加えて同一初期重み・同一バッチで **Python と C++ の loss/勾配が 1e-4 以内**、両者の出力 ONNX が相互に 1e-5 以内で一致。
- [ ] **M5 4隅回帰器（両言語）** — 合成主体で学習 → ONNX 出力。
      完了条件: 合成 val で 4隅平均誤差 ≤ プレート幅の 1.5%、実データで warp 後の認識精度が M4 を下回らないこと。loss パリティも同上。
- [ ] **M6 検出器（両言語）** — `yolov8n.pt` から nc=1 で転移（実データ＋合成コンポジット＋ハードネガ）。
      C++ 側は **cls head の 80 クラス固定を解消**する必要がある（`yolov8_cpp` RESUME #2）。
      完了条件: 実データ hold-out で mAP@0.5 ≥ 0.95、**色別 recall（白/黄/緑/黒）すべて ≥ 0.90**、近接（画面幅 40% 超）でも検出。
      C++ と Python で v8 loss が 1e-4 以内、val mAP が 1e-3 以内。
- [ ] **M7 推論パイプライン（両言語）** — ONNX リーダ／decode+NMS（nc 一般化）／warp／11 head decode／CLI。
      完了条件: 同一画像で **C++ と Python が同一文字列**、スコア差 1e-4 以内。実写フィクスチャで回帰テスト。
- [ ] **M8 WASM デモ** — カメラ＋ファイル入力、3 モデル fetch、注釈 PNG 保存、GitHub Pages。
      完了条件: 実写 3 枚で正しく読め、CLI と同じ文字列。1 フレームの実行時間を計測して README に載せる。
- [ ] **M9 リリース** — `models/` に 3 つの ONNX、両言語の `val` が同じ数値を出す、色別・地名別・解像度別の評価表を README に掲載。

## 速度の非対等（承知の上）

姉妹リポの実測: C++/CUDA 経路は COCO128/640/batch4 で **約 4.6 分/epoch**（100 epoch ≒ 8 時間）、
CPU 640px は丸一日規模。よって**本番の長時間学習は Python/CUDA で回す**のが現実的で、C++ 側は
「同じ初期値・同じデータで同じところに向かう」ことを小規模で証明し、追加微調整に使える状態を保つ。
機能のパリティは必須、速度のパリティは求めない。

## 次の一手

**M1（ラベル表の確定）と M2（生成器）** から。モデルを触る前にラベル定義を固定しないと、
学習をやり直す羽目になる。M1 は現行地名リストの出典確認（Wikipedia の地域名一覧＋国交省の図柄ナンバー一覧）を含む。

## 未確認事項（着手前に潰す）

1. **Roboflow の日本プレート dataset のライセンス** — `universe.roboflow.com` が 403 で未取得。
   商用不可なら検出は Open Images + 合成 + 自前撮影に切り替える（方針自体は変わらない）。
2. **FGゼロラバウルの再配布可否** — 「個人・商用利用無料」は確認済みだが、**フォントファイルをリポジトリに含めて
   良いか**は同梱 ReadMe を読むまで不明。原則リポには置かず、`tools/gen/fetch_fonts.py` で取得させる。
3. **地名の現行リスト** — 133 → 138+ の確定（廃止・改称の有無も含む）。図柄入りは「地名は同じで背景が違う」だけの
   はずだが、字体・字配置が変わるものがないか確認。
4. **図柄入りナンバーの背景をどう合成するか** — 実物の図柄は著作権があるので**模倣柄で近似**する方針。
   実写での通用性は M4 の評価で測る。
5. **plate_kind head の切り方** — 8 クラス 1 本で足りるか、色 / 大型 / 図柄を独立 head に割るか。
   M2 の生成器で両方のラベルを出しておけば後から選べる。
6. **WASM の実行時間** — `lpr_cpp` の YOLOX tiny は 416px SIMD で 3.3s/frame だった。yolov8n 640 は更に重い可能性が高い。
   416/512 の ONNX を先に用意し、体感で決める。

## ログ

- **2026-08-19** — 方針決定。3段パイプライン（検出 yolov8n nc=1 → 4隅補正 → 11 head 分類）、
  成果物は全段 ONNX、対応は中型＋大型＋軽＋図柄入り、事前学習は AGPL 許容、WASM デモ込み。
  リポジトリ作成、README / RESUME 起稿。
- **2026-08-19（追記）** — 「**Python と C++ でどちらでも同じことができる**」を設計の核に昇格。
  生成・学習・推論・評価・ONNX 入出力を両言語に持たせ、`spec/` を唯一の定義とし、パリティテストで縛る。
  マイルストーンを 3 点セット（Python 実装・C++ 実装・パリティ）方式に組み替え。
