# RESUME — yolo_lpr_cpp の進捗と残作業

方針は [README.md](README.md)。ここは**現在地・次の一手・未確認事項**を書き続けるファイル。

## 現在地（2026-08-19）

**ワンパス推論が CLI と WASM の両方で動いている。** 既存の学習済みモデル（暫定の検出器＋実データ学習済みの
認識器）で、実写 1 枚から `横浜 200 か 3591` が読める。学習系（合成データ生成・ファインチューニング・
4隅回帰・yolov8 への差し替え）はこれから。

```
実写 960x640 ──▶ 検出 PlateYOLO-JP(320, 暫定・借り物) ──▶ box ──▶ 6クロップ ──▶ 9 head ──▶ 横浜 200 か 3591
CLI  2.4 秒 / WASM(node, SIMD) 4.5 秒 ── 同じ画素で同じ文字列・同じ box (±8px)
```

| 検証 | 結果 |
|---|---|
| ラベル表パリティ（C++ ⇔ Python） | dump 9,019 bytes / emit-header 7,383 bytes が **完全一致**（MSVC・g++・Python の 3 者） |
| 出力した ONNX vs 元 ONNX（onnxruntime 出力の fixture） | worst **3.299e-05**、argmax **9/9** — lpr_cpp の 3.3e-05 と同水準 |
| CLI 実写 | `横浜 200 か 3591`（det 0.80、地域名 conf 0.92） |
| WASM 実写（node） | 同じ文字列・同じ box（±8px 以内を assert）、4.48 秒 |
| 認識器の実データ比較（720枚・地域名） | 自前 **92.5%**（TTA）/ 89.2%（単一） vs EkMixer 81.2% / 76.1% → **自前を base に採用** |
| 検出器の差し替え効果（同じ写真・同じ認識器） | YOLOX 暫定: 誤検出1件・地域名 conf 0.57・5.6秒 → PlateYOLO-JP: **0件・0.92・2.4秒** |
| ビルド | MSVC 14.41（vcvars 不要の `build/cc.sh`）・g++ 14.2・emcc（`-msimd128`）の 3 系統 |
| Python 推論との一致（M3） | 強い box は文字列・argmax 一致、box 差 **0.10px**、det 差 **0.0000**、地域名 conf 差 **0.0108** |
| 出力 ONNX を onnxruntime で読んだ結果 vs 元 ONNX fixture | worst **7.451e-09** — エクスポータは実質完全。自作インタプリタ側の 3.3e-05 は float 加算順序 |

**自作 ONNX インタプリタの数値誤差**（測って分かったこと）: 認識器（27 conv）で **3.3e-05**、
検出器（201 ノード・416×416）で **約 3e-03**。onnxruntime は同じ ONNX で 7.5e-09 なので、
差は自作側の naive な float 加算順序。強い検出には影響しないが、**閾値ぎりぎりの box では
読みが両実装で変わる**（実例: det 0.24 の誤検出が C++ 「なにわ」/ Python 「鹿児島」）。

**今すぐ効く弱点**（数字が出ているので直す優先度が判断できる）:
- 地域名 head の conf は 0.92 まで来た（検出器の差し替えで 0.57 から改善）が、まだ 1.00 ではない。→ 4隅補正（M6）。
- 検出器が**借り物**（PlateYOLO-JP, AGPL-3.0）。自前 yolov8n がこれを超えるのが M7 の合格条件。
- 近接・手持ちのプレートは依然として検出器の苦手領域なので、WASM デモの**手動枠**は残す。

調査で分かっている前提（再調査しないこと）:
- `yolov8_cpp` は v5/v8/v11/x 全サイズ、`yolo` CLI、`.pt` 読み書き、ONNX、CUDA 経路まで揃っている。
  **ただし cls head が 80 クラス固定**（同リポ RESUME #2）→ C++ 側でも nc=1 を学習するので**必須作業**（M7）。
- `lpr_cpp` は 9-head 分類器の forward parity 3.3e-05、pure C++ 学習ループ、実データローダ、WASM デモ、
  Eigen で約2倍。重み `pure/ref/weights.bin` は実データ学習済みの ONNX 由来（**元の .onnx は非公開**なので、
  本リポでは `jlpr export` で weights.bin から ONNX を作り直した）。
- 実データの当たり: `dyama/alpr_jp`(MIT) の `自家用/<地名>/` 720枚が**地域名ラベル付き**（66地名）、
  他に無ラベル 529枚、ネガ 4,420枚。
- 生成用書体の当たり: FGゼロラバウル（プレート書体再現・漢字かな入り・商用可フリー）。
- 地域名は現行 138 件（2025-05 に 十勝・日光・江戸川・安曇野・南信州 が追加）。既存 133 は完全な部分集合。
- emsdk は `C:\prog\emsdk\emsdk` に **install 済み**（`upstream/emscripten/emcc.py`、node は
  `node/22.16.0_64bit/bin/node.exe`）。`build/emcc.sh` がそこを直接叩く。

## マイルストーン

各段の「完了」は**数値で言えること**を条件にする。学習が絡む段は
**Python 実装・C++ 実装・両者のパリティ**の 3 点セットで完了とする（速度のパリティは求めない）。

- [x] **M0 足場** — `spec/` `pure/` `tools/` `wasm/` `models/` `build/`、MSVC/g++/emcc の 3 系統、stb 同梱。
- [x] **M1 ラベル表** — `spec/labels.txt` を実行時に読む方式（C++ `pure/spec.hpp` / Python `tools/labels.py`）。
      現行地名 138 を出典付きで固定、11 head ＋ 生成器専用ラベルを定義。
      パリティ: dump と emit-header が **バイト一致**（`python tools/parity/labels.py`）。
- [x] **M2 ワンパス推論（既存モデル・CLI + WASM）** — 統合 ONNX ランナ、weights.bin→ONNX エクスポータ、
      detect→crop→11head decode、ブラウザ demo（カメラ/ファイル/手動枠/PNG保存）と node テスト。
      数値は上の表。
- [x] **M3 Python 側の推論を対等にする** — `tools/infer.py`（onnxruntime）＋ `tools/jlpr.py detect`、
      前処理は `pure/crop.hpp` から式ごと移植。`jlpr detect --json` と JSON を突き合わせる
      `tools/parity/infer.py` を追加（head レベル＋ pipeline レベル）。**PASS**:
      強い box は 文字列・argmax 一致、box 差 0.10px、det 差 0.0000、地域名 conf 差 0.0108。
- [x] **M3.5 参考実装の評価と取り込み** — [PlateYOLO-JP](https://github.com/Kazuhito00/PlateYOLO-JP-Prototype)
      （YOLO12 検出器 + EkMixer 認識器, AGPL-3.0）を評価。認識器は自前が明確に勝ったので base 継続、
      検出器は借りて暫定に採用（NMS を `tools/strip_nms.py` で剥がし、`pure/nd.hpp` + `pure/infer_v8.hpp` を
      追加して自作ランナで実行）。副産物: 外部モデルのラベル空間を同じ形式で扱う `spec/ekmixer_labels.txt`、
      実データ評価ハーネス `tools/eval_ocr.py`。
- [ ] **M4 合成データ生成器（両言語）** — `tools/gen.py` と `pure/gen.cpp`。テンプレート（中型/大型/図柄入り）、
      書体、物理劣化、4隅 GT、全 head ラベル。同じ seed で**文字列・4隅・変換パラメータ・色・種別が完全一致**
      （画像はラスタライザ差を許容し統計＋目視）。生成→デコードで元の文字列に戻る自動チェック。
- [ ] **M5 認識器の学習（両言語）** — 合成事前学習 → head を 9→11 に拡張（region も 133→138、append-only）
      → 実データ微調整 → 両言語から ONNX 出力。
      完了条件: 実データ hold-out で全文一致 ≥ 95%、region ≥ 98%（TTA なし）。
      同一初期重み・同一バッチで **loss/勾配が 1e-4 以内**、出力 ONNX が相互に 1e-5 以内。
- [ ] **M6 4隅回帰器（両言語）** — 合成主体で学習 → ONNX。
      完了条件: 合成 val で 4隅平均誤差 ≤ プレート幅の 1.5%、実写で **地域名 conf が 0.57 から上がる**こと。
- [ ] **M7 検出器を自前 yolov8 nc=1 に差し替え** — `yolov8n.pt` から転移。学習データは
      **PlateYOLO-JP を教師にした自動ラベル**（Python 側で車写真に box を付ける）＋合成コンポジット＋ハードネガ。
      C++ 側は cls head 80 固定の解消が必要（`yolov8_cpp` RESUME #2）。ONNX は NMS 抜きで出す
      （`pure/infer_v8.hpp` がそのまま使える）。
      完了条件: 実データ hold-out で mAP@0.5 ≥ 0.95、**色別 recall（白/黄/緑/黒）すべて ≥ 0.90**、
      **プレートが画面幅の 5%〜95% のどこにあっても検出**（借り物は 20% で 0.07、30% 以上は 0 — 上の実測表）、
      そして**借り物の PlateYOLO-JP と同等以上**であること。
- [ ] **M8 WASM 仕上げ** — GitHub Pages 公開、416/512 のモデル併置、1 フレームの実測を README に掲載、
      ブラウザ（実機）での動作確認。今は node での動作しか確認していない。
- [ ] **M9 リリース** — `models/` に 3 つの ONNX、両言語の `val` が同じ数値、色別・地名別・解像度別の評価表。

## 次の一手

1. **ブラウザ実機での確認**（`python -m http.server` → `wasm/index.html`）。node は通ったがブラウザは未確認。
2. **M4**（合成データ生成器）。学習系すべての前提。ここから両言語 3 点セットに戻る。
3. その次は M5（認識器の学習：合成事前学習 → 11 head 化 → 実データ微調整）→ M6（4隅）→ M7（自前検出器）。
   M5 の出発点はもう測れている: 実データ 720 枚で region 92.5%（TTA）/ 89.2%（単一）。ここを上げる。

## 検出器は「車体込みの小さいプレート」しか取れない — 2026-08-19 実測

`tools/context_test.py`（同じ写真、プレートを中心に切り出す窓の大きさだけを変える）:

| プレートが窓幅に占める割合 | 5% | 8% | 12% | 20% | 30% | 45%以上 |
|---|---|---|---|---|---|---|
| 現行 PlateYOLO-JP 320 のスコア | 0.757 | **0.826** | 0.695 | 0.069 | なし | なし |

**借り物の検出器 2 つとも同じ性質**（旧 YOLOX も最良帯 5–12%）で、これは学習データの分布そのもの。
つまり手持ちで寄って撮ると検出できないのは仕様どおりで、閾値を下げても直らない。

- WASM デモ: 実測帯をページに明記し、「枠を手で指定」を常設（認識器は近接でも読める）。
- M7 の合格条件に **画面幅 5%–95% のどこでも検出**を追加。合成コンポジットで近接も学習させる。

## プレート色（黒ナンバー）の現状 — 2026-08-19 実測

`tools/recolor_test.py`（同じ写真・同じ幾何、プレート内の輝度を min-max 正規化して地色/文字色を差し替え、
陰影とにじみは保存）:

| 地色/文字色 | 現行 PlateYOLO-JP 320 | 旧 YOLOX-tiny 416 | 同じ box を渡したときの読み |
|---|---|---|---|
| 白地緑字（自家用） | 0.796 | 0.800 | 横浜 200 か 3591（地域名 conf 0.91） |
| 緑地白字（事業用） | 0.799 | 0.722 | 同（1.00） |
| 黄地黒字（軽自家用） | 0.721 | 0.645 | 同（0.97） |
| **黒地黄字（軽事業用）** | **0.835** | 0.720 | 同（0.99） |

結論を 2 つに分けて書く:
- **認識器は 4 色すべて問題なく読めている**（同じ box を渡せば conf 0.91-1.00）。色は認識側のボトルネックではない。
- **検出器の色バイアスは、この方法では再現しない**。lpr_cpp が報告した「黒 0.21」は当時の色替え方法か写真に
  依存していた可能性が高く、現行検出器の 0.835 を「直った」と言い切るのは早い。判定には**実写の黒/黄ナンバー**が必要
  （データ不足。撮影 1-2 枚あれば決着する）。

## 未確認事項（着手前に潰す）

1. **Roboflow の日本プレート dataset のライセンス** — `universe.roboflow.com` が 403 で未取得。
   商用不可なら検出は Open Images + 合成 + 自前撮影に切り替える（方針自体は変わらない）。
2. **FGゼロラバウルの再配布可否** — 「個人・商用利用無料」は確認済みだが、**フォントファイルをリポジトリに
   含めて良いか**は同梱 ReadMe を読むまで不明。原則リポには置かず、`tools/gen/fetch_fonts.py` で取得させる。
3. ~~**地名の現行リスト**~~ — **解決（2026-08-19）**: 現行 138 件（うちご当地 51 件）を Wikipedia
   「日本のナンバープレート地域名一覧」から機械抽出。既存 133 は完全な部分集合で廃止・改称なし、追加は
   **十勝・日光・江戸川・安曇野・南信州** の 5 件だけ。index 0..132 固定＋末尾追記で事前学習 head を活かせる。
   残る確認: 図柄入りで字体・字配置が変わるものがないか（M4 の生成器で実物と見比べる）。
4. **図柄入りナンバーの背景をどう合成するか** — 実物の図柄は著作権があるので**模倣柄で近似**する方針。
   実写での通用性は M5 の評価で測る。
5. **plate_kind head の切り方** — 8 クラス 1 本で足りるか、色 / 大型 / 図柄を独立 head に割るか。
   M4 の生成器で両方のラベルを出しておけば後から選べる。
6. **WASM の速度** — node/SIMD で detect(320)+6クロップ = 4.48 秒（CLI は 2.4 秒）。`nd.hpp` の N次元 op が
   素朴なループなので、SIMD が効いていない可能性が高い。ブラウザ実測とプロファイルが必要。
   TTA を切る（=4隅補正を入れる）と認識側は 1/6 になる。
7. ~~**20 MB の検出器**~~ — 解決: 320px 版に替えて **10 MB**。fp16 化でさらに半分にできる（M8 で検討）。
8. **借り物の検出器をどう扱うか** — 暫定として `models/` に置いた（AGPL-3.0、`THIRD_PARTY_NOTICES.md` に明記）。
   M7 で自前が超えたら差し替え、超えられなければ「超えられなかった」と数字で書く。

## ログ

- **2026-08-19** — 方針決定。3段パイプライン（検出 yolov8n nc=1 → 4隅補正 → 11 head 分類）、
  成果物は全段 ONNX、対応は中型＋大型＋軽＋図柄入り、事前学習は AGPL 許容、WASM デモ込み。
  リポジトリ作成、README / RESUME 起稿。
- **2026-08-19（追記）** — 「**Python と C++ でどちらでも同じことができる**」を設計の核に昇格。
  `spec/` を唯一の定義とし、パリティテストで縛る。マイルストーンを 3 点セット方式に組み替え。
- **2026-08-19（M1）** — `spec/labels.txt` 起草（11 head ＋ 生成器専用ラベル）。現行地名 138 を一次情報から確定。
  `class_num_03` は既存モデル由来の未使用 1 枠があり（トークン 21 に対し 22 クラス）、互換のため `<unused>` で残す。
- **2026-08-19（M0/M1 完了）** — エンジンを姉妹リポから移植、`pure/spec.hpp` ＋ `tools/labels.py` を
  実行時パース方式で実装、パリティ PASS。`build/cc.sh` が vcvars なしで MSVC を叩けることを確認。
- **2026-08-19（M2 完了・最初のゴール）** — ワンパス推論。統合 ONNX ランナ、`jlpr export` で
  weights.bin→ONNX（元 ONNX と 3.299e-05 一致）、`jlpr detect` で実写を読み、同じコードを WASM 化して
  node で同一結果。**優先順位を「まず WASM で推論できるワンパス」に変更したのに合わせ、学習系は M4 以降へ後ろ倒し。**
- **2026-08-19（M3 完了）** — Python 推論（onnxruntime）を追加して両言語で同じ ONNX・同じ decode に。
  パリティを 2 段構えにした: **head レベル**（fixture 相手。ORT 7.451e-09 で、エクスポータの正しさと
  自作インタプリタの誤差 3.3e-05 を分離できた）と **pipeline レベル**（JSON 突き合わせ）。
  低スコア box で読みが割れるのは地域名 head の不安定さそのもので、M6 の 4隅補正で潰す対象として記録。
- **2026-08-19（M3.5）** — 参考実装 PlateYOLO-JP-Prototype を評価。**認識器は自前が勝った**
  （720 枚実データの地域名 top1: 自前 92.5% vs EkMixer 81.2%、TTA 時）ので base は変えない。
  **検出器は借りた**（YOLO12, AGPL）: NMS 入りの ONNX は自作ランナで実行不能だったので、NMS 手前で
  グラフを切って（`tools/strip_nms.py`）N次元 op（`pure/nd.hpp`）と v8 decode（`pure/infer_v8.hpp`）を
  実装して実行。結果、同じ写真で誤検出 1→0 件、地域名 conf 0.57→0.92、CLI 5.6→2.4 秒。
  **box の質が認識精度を支配する**ことが数字で出たので、M6（4隅補正）の位置づけがより明確になった。
  外部モデルのラベル空間（region 139・index 6 未使用）も現行 138 地名と完全一致で、我々のリストの独立確認になった。
