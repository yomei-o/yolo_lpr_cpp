# RESUME — yolo_lpr_cpp の進捗と残作業

方針は [README.md](README.md)。ここは**現在地・次の一手・未確認事項**を書き続けるファイル。

## 現在地（2026-08-19）

技術方針の確定まで。コードはまだ 1 行も無い。確定内容は README の「確定した方針」表。
調査で分かっている前提（再調査しないこと）:

- `yolov8_cpp` は v5/v8/v11/x 全サイズ、`yolo` CLI（train/val/detect, data.yaml, mosaic/mixup/HSV/affine）、
  `.pt` 読み書き、ONNX リーダ／エクスポータ、CUDA(Thrust/cuBLAS) 経路まで揃っている。
  **ただし cls head が 80 クラス固定**（同リポ RESUME #2）→ C++ 側で nc=1 を学習するならここが必須作業。
- `lpr_cpp` は 9-head 分類器の forward parity 3.3e-05、pure C++ 学習ループ、実データローダ（`labels.txt`）、
  WASM デモ、Eigen で約2倍、`lpr_infer` まで動いている。重み `pure/ref/weights.bin` は実データ学習済みの ONNX 由来。
- 実データの当たり: `dyama/alpr_jp`(MIT) の `自家用/<地名>/` 720枚が**地域名ラベル付き**（66地名）、
  他に無ラベル 529枚、ネガ 4,420枚。
- 生成用書体の当たり: FGゼロラバウル（プレート書体再現・漢字かな入り・商用可フリー）。
- 地域名は 2025-05 に 十勝・日光・江戸川・安曇野・南信州 が追加。既存 133 クラスは拡張が必要。

## マイルストーン

各段の「完了」は**数値で言えること**を条件にする（姉妹リポと同じ流儀）。

- [ ] **M0 足場** — リポ骨格（`pure/` `tools/` `wasm/` `models/`）、ビルドスクリプト（cl / g++ / emcc）、
      stb ほか同梱物、`.gitignore`。完了条件: 空の `jlpr` が 3 系統でビルドできる。
- [ ] **M1 ラベル表の確定** — 現行地名リスト（138+）を出典付きで固定、ひらがな使用可集合、分類番号の桁ルール、
      種別 8 クラスを `pure/lpr_labels.hpp` と Python 側で**同一の生成物**として持つ。
      完了条件: 既存 133 との差分が列挙され、C++/Python のラベル表がバイト一致で突き合わせできる。
- [ ] **M2 合成データ生成器**（Python, `tools/gen/`） — テンプレート・書体・物理劣化・4隅 GT・全 head ラベル。
      完了条件: 認識用 crop と検出用コンポジットを各 1,000 枚出し、目視で「本物と混ぜて区別しづらい」水準＋
      ラベルの整合を自動チェック（生成→デコード→元の文字列に戻る）。
- [ ] **M3 認識器の PyTorch 再実装＋既存重みロード** — `ARCH.md` 通りに組み、既存 ONNX から重みを移す。
      完了条件: 既存 ONNX と **9 head 出力が 1e-5 以内**で一致（`lpr_cpp` の 3.3e-05 と同水準）。
- [ ] **M4 認識器の学習** — 合成事前学習 → head 拡張（11 head）→ 実データ微調整 → ONNX 出力。
      完了条件: 実データ hold-out でプレート全文一致 ≥ 95%、region ≥ 98%（TTA なし）。色別・解像度別の内訳も出す。
- [ ] **M5 4隅回帰器** — 合成主体で学習 → ONNX 出力。完了条件: 合成 val で 4隅平均誤差 ≤ プレート幅の 1.5%、
      実データで warp 後の認識精度が M4 より落ちないこと（＝2段目が効いている証明）。
- [ ] **M6 検出器の学習** — `yolov8n.pt` から nc=1 で転移（実データ＋合成コンポジット＋ハードネガ）→ ONNX 出力。
      完了条件: 実データ hold-out で mAP@0.5 ≥ 0.95、**色別 recall（白/黄/緑/黒）すべて ≥ 0.90**、
      近接（画面幅 40% 超）でも検出できること。
- [ ] **M7 C++ 推論パイプライン** — ONNX リーダ／decode+NMS（nc 一般化）／warp／11 head decode／CLI。
      完了条件: Python 側の推論結果と**同一画像で同一文字列**、スコア差 1e-4 以内。実写フィクスチャで回帰テスト。
- [ ] **M8 WASM デモ** — カメラ＋ファイル入力、3 モデル fetch、注釈 PNG 保存、GitHub Pages。
      完了条件: 実写 3 枚で正しく読め、1 フレームの実行時間を計測して README に載せる。
- [ ] **M9 C++ 従の学習パス** — 認識器の追加学習（`lpr_cpp` の autograd 移植）＋ weights→ONNX エクスポータ、
      検出器の nc=1 対応（`yolov8_cpp` RESUME #2）。完了条件: 小さな実データで loss が下がり、
      出力 ONNX が M7 のパイプラインでそのまま動く。

## 次の一手

**M1（ラベル表の確定）と M2（生成器）** から。モデルを触る前にラベル定義を固定しないと、
学習をやり直す羽目になる。M1 は現行地名リストの出典確認（Wikipedia の地域名一覧＋国交省の図柄ナンバー一覧）を含む。

## 未確認事項（着手前に潰す）

1. **Roboflow の日本プレート dataset のライセンス** — `universe.roboflow.com` が 403 で未取得。
   商用不可なら検出は Open Images + 合成 + 自前撮影に切り替える（方針自体は変わらない）。
2. **FGゼロラバウルの再配布可否** — 「個人・商用利用無料」は確認済みだが、**フォントファイルをリポジトリに含めて
   良いか**は同梱 ReadMe を読むまで不明。原則リポには置かず、`tools/gen/fetch_fonts.py` で取得させる。
3. **地名の現行リスト** — 133 → 138+ の確定（廃止・改称の有無も含む）。図柄入りは「地名は同じで背景が違う」だけの
   はずだが、字体・字配置が変わるものがないか確認。
4. **図柄入りナンバーの背景をどう合成するか** — 実物の図柄は著作権があるので**模倣柄で近似**する方針。
   実写での通用性は M4 の評価で測る。
5. **plate_kind head の切り方** — 8 クラス 1 本で足りるか、色 / 大型 / 図柄を独立 head に割るか。
   M2 の生成器で両方のラベルを出しておけば後から選べる。
6. **WASM の実行時間** — `lpr_cpp` の YOLOX tiny は 416px SIMD で 3.3s/frame だった。yolov8n 640 は更に重い可能性が高い。
   416/512 の ONNX を先に用意し、体感で決める。

## ログ

- **2026-08-19** — 方針決定。3段パイプライン（検出 yolov8n nc=1 → 4隅補正 → 11 head 分類）、
  学習は Python 主／C++ 従、成果物は全段 ONNX、対応は中型＋大型＋軽＋図柄入り、事前学習は AGPL 許容。
  リポジトリ作成、README / RESUME 起稿。
